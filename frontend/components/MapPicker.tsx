import React from "react";
import { StyleSheet, View, Text } from "react-native";

const MapPicker = () => {
  return (
    <View style={styles.container}>
      <Text>MapPicker Component</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default MapPicker;
